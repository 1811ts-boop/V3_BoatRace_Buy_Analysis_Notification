name: V11 Daily Inference Run

on:
  # 手動実行用トリガー
  workflow_dispatch:

  # スケジュール実行用トリガー（例：毎日日本時間 朝8時 = UTC 23時）
  # 自動実行させる場合は下の2行のコメントアウト(#)を外してください
  # schedule:
  #   - cron: '0 23 * * *'

jobs:
  run-inference:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 必要なライブラリをインストール
          pip install requests pandas numpy lightgbm scikit-learn beautifulsoup4 google-api-python-client google-auth google-auth-oauthlib google-auth-httplib2
          
      - name: Run V11 Inference Script
        env:
          # GitHub Secrets に登録した環境変数をスクリプトに渡す
          GCP_SA_CREDENTIALS: ${{ secrets.GCP_SA_CREDENTIALS }}
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: |
          # 先ほど作成したV11の実行ファイル名を指定してください
          python V11_DailyRun_Batch.py
